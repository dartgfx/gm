// GENERATED CODE - DO NOT MODIFY BY HAND
// Run: dart tool/generate_swizzle.dart

import 'vec.dart';

// ignore_for_file: camel_case_extensions

extension vec2Swizzle on vec2 {
  vec2 get xx => vec2(x, x);
  vec2 get xy => vec2(x, y);
  vec2 get yx => vec2(y, x);
  vec2 get yy => vec2(y, y);
  vec2 get rr => vec2(x, x);
  vec2 get rg => vec2(x, y);
  vec2 get gr => vec2(y, x);
  vec2 get gg => vec2(y, y);
}

extension vec3Swizzle on vec3 {
  vec2 get xx => vec2(x, x);
  vec2 get xy => vec2(x, y);
  vec2 get xz => vec2(x, z);
  vec2 get yx => vec2(y, x);
  vec2 get yy => vec2(y, y);
  vec2 get yz => vec2(y, z);
  vec2 get zx => vec2(z, x);
  vec2 get zy => vec2(z, y);
  vec2 get zz => vec2(z, z);
  vec2 get rr => vec2(x, x);
  vec2 get rg => vec2(x, y);
  vec2 get rb => vec2(x, z);
  vec2 get gr => vec2(y, x);
  vec2 get gg => vec2(y, y);
  vec2 get gb => vec2(y, z);
  vec2 get br => vec2(z, x);
  vec2 get bg => vec2(z, y);
  vec2 get bb => vec2(z, z);
  vec3 get xxx => vec3(x, x, x);
  vec3 get xxy => vec3(x, x, y);
  vec3 get xxz => vec3(x, x, z);
  vec3 get xyx => vec3(x, y, x);
  vec3 get xyy => vec3(x, y, y);
  vec3 get xyz => vec3(x, y, z);
  vec3 get xzx => vec3(x, z, x);
  vec3 get xzy => vec3(x, z, y);
  vec3 get xzz => vec3(x, z, z);
  vec3 get yxx => vec3(y, x, x);
  vec3 get yxy => vec3(y, x, y);
  vec3 get yxz => vec3(y, x, z);
  vec3 get yyx => vec3(y, y, x);
  vec3 get yyy => vec3(y, y, y);
  vec3 get yyz => vec3(y, y, z);
  vec3 get yzx => vec3(y, z, x);
  vec3 get yzy => vec3(y, z, y);
  vec3 get yzz => vec3(y, z, z);
  vec3 get zxx => vec3(z, x, x);
  vec3 get zxy => vec3(z, x, y);
  vec3 get zxz => vec3(z, x, z);
  vec3 get zyx => vec3(z, y, x);
  vec3 get zyy => vec3(z, y, y);
  vec3 get zyz => vec3(z, y, z);
  vec3 get zzx => vec3(z, z, x);
  vec3 get zzy => vec3(z, z, y);
  vec3 get zzz => vec3(z, z, z);
  vec3 get rrr => vec3(x, x, x);
  vec3 get rrg => vec3(x, x, y);
  vec3 get rrb => vec3(x, x, z);
  vec3 get rgr => vec3(x, y, x);
  vec3 get rgg => vec3(x, y, y);
  vec3 get rgb => vec3(x, y, z);
  vec3 get rbr => vec3(x, z, x);
  vec3 get rbg => vec3(x, z, y);
  vec3 get rbb => vec3(x, z, z);
  vec3 get grr => vec3(y, x, x);
  vec3 get grg => vec3(y, x, y);
  vec3 get grb => vec3(y, x, z);
  vec3 get ggr => vec3(y, y, x);
  vec3 get ggg => vec3(y, y, y);
  vec3 get ggb => vec3(y, y, z);
  vec3 get gbr => vec3(y, z, x);
  vec3 get gbg => vec3(y, z, y);
  vec3 get gbb => vec3(y, z, z);
  vec3 get brr => vec3(z, x, x);
  vec3 get brg => vec3(z, x, y);
  vec3 get brb => vec3(z, x, z);
  vec3 get bgr => vec3(z, y, x);
  vec3 get bgg => vec3(z, y, y);
  vec3 get bgb => vec3(z, y, z);
  vec3 get bbr => vec3(z, z, x);
  vec3 get bbg => vec3(z, z, y);
  vec3 get bbb => vec3(z, z, z);
}

extension vec4Swizzle on vec4 {
  vec2 get xx => vec2(x, x);
  vec2 get xy => vec2(x, y);
  vec2 get xz => vec2(x, z);
  vec2 get xw => vec2(x, w);
  vec2 get yx => vec2(y, x);
  vec2 get yy => vec2(y, y);
  vec2 get yz => vec2(y, z);
  vec2 get yw => vec2(y, w);
  vec2 get zx => vec2(z, x);
  vec2 get zy => vec2(z, y);
  vec2 get zz => vec2(z, z);
  vec2 get zw => vec2(z, w);
  vec2 get wx => vec2(w, x);
  vec2 get wy => vec2(w, y);
  vec2 get wz => vec2(w, z);
  vec2 get ww => vec2(w, w);
  vec2 get rr => vec2(x, x);
  vec2 get rg => vec2(x, y);
  vec2 get rb => vec2(x, z);
  vec2 get ra => vec2(x, w);
  vec2 get gr => vec2(y, x);
  vec2 get gg => vec2(y, y);
  vec2 get gb => vec2(y, z);
  vec2 get ga => vec2(y, w);
  vec2 get br => vec2(z, x);
  vec2 get bg => vec2(z, y);
  vec2 get bb => vec2(z, z);
  vec2 get ba => vec2(z, w);
  vec2 get ar => vec2(w, x);
  vec2 get ag => vec2(w, y);
  vec2 get ab => vec2(w, z);
  vec2 get aa => vec2(w, w);
  vec3 get xxx => vec3(x, x, x);
  vec3 get xxy => vec3(x, x, y);
  vec3 get xxz => vec3(x, x, z);
  vec3 get xxw => vec3(x, x, w);
  vec3 get xyx => vec3(x, y, x);
  vec3 get xyy => vec3(x, y, y);
  vec3 get xyz => vec3(x, y, z);
  vec3 get xyw => vec3(x, y, w);
  vec3 get xzx => vec3(x, z, x);
  vec3 get xzy => vec3(x, z, y);
  vec3 get xzz => vec3(x, z, z);
  vec3 get xzw => vec3(x, z, w);
  vec3 get xwx => vec3(x, w, x);
  vec3 get xwy => vec3(x, w, y);
  vec3 get xwz => vec3(x, w, z);
  vec3 get xww => vec3(x, w, w);
  vec3 get yxx => vec3(y, x, x);
  vec3 get yxy => vec3(y, x, y);
  vec3 get yxz => vec3(y, x, z);
  vec3 get yxw => vec3(y, x, w);
  vec3 get yyx => vec3(y, y, x);
  vec3 get yyy => vec3(y, y, y);
  vec3 get yyz => vec3(y, y, z);
  vec3 get yyw => vec3(y, y, w);
  vec3 get yzx => vec3(y, z, x);
  vec3 get yzy => vec3(y, z, y);
  vec3 get yzz => vec3(y, z, z);
  vec3 get yzw => vec3(y, z, w);
  vec3 get ywx => vec3(y, w, x);
  vec3 get ywy => vec3(y, w, y);
  vec3 get ywz => vec3(y, w, z);
  vec3 get yww => vec3(y, w, w);
  vec3 get zxx => vec3(z, x, x);
  vec3 get zxy => vec3(z, x, y);
  vec3 get zxz => vec3(z, x, z);
  vec3 get zxw => vec3(z, x, w);
  vec3 get zyx => vec3(z, y, x);
  vec3 get zyy => vec3(z, y, y);
  vec3 get zyz => vec3(z, y, z);
  vec3 get zyw => vec3(z, y, w);
  vec3 get zzx => vec3(z, z, x);
  vec3 get zzy => vec3(z, z, y);
  vec3 get zzz => vec3(z, z, z);
  vec3 get zzw => vec3(z, z, w);
  vec3 get zwx => vec3(z, w, x);
  vec3 get zwy => vec3(z, w, y);
  vec3 get zwz => vec3(z, w, z);
  vec3 get zww => vec3(z, w, w);
  vec3 get wxx => vec3(w, x, x);
  vec3 get wxy => vec3(w, x, y);
  vec3 get wxz => vec3(w, x, z);
  vec3 get wxw => vec3(w, x, w);
  vec3 get wyx => vec3(w, y, x);
  vec3 get wyy => vec3(w, y, y);
  vec3 get wyz => vec3(w, y, z);
  vec3 get wyw => vec3(w, y, w);
  vec3 get wzx => vec3(w, z, x);
  vec3 get wzy => vec3(w, z, y);
  vec3 get wzz => vec3(w, z, z);
  vec3 get wzw => vec3(w, z, w);
  vec3 get wwx => vec3(w, w, x);
  vec3 get wwy => vec3(w, w, y);
  vec3 get wwz => vec3(w, w, z);
  vec3 get www => vec3(w, w, w);
  vec3 get rrr => vec3(x, x, x);
  vec3 get rrg => vec3(x, x, y);
  vec3 get rrb => vec3(x, x, z);
  vec3 get rra => vec3(x, x, w);
  vec3 get rgr => vec3(x, y, x);
  vec3 get rgg => vec3(x, y, y);
  vec3 get rgb => vec3(x, y, z);
  vec3 get rga => vec3(x, y, w);
  vec3 get rbr => vec3(x, z, x);
  vec3 get rbg => vec3(x, z, y);
  vec3 get rbb => vec3(x, z, z);
  vec3 get rba => vec3(x, z, w);
  vec3 get rar => vec3(x, w, x);
  vec3 get rag => vec3(x, w, y);
  vec3 get rab => vec3(x, w, z);
  vec3 get raa => vec3(x, w, w);
  vec3 get grr => vec3(y, x, x);
  vec3 get grg => vec3(y, x, y);
  vec3 get grb => vec3(y, x, z);
  vec3 get gra => vec3(y, x, w);
  vec3 get ggr => vec3(y, y, x);
  vec3 get ggg => vec3(y, y, y);
  vec3 get ggb => vec3(y, y, z);
  vec3 get gga => vec3(y, y, w);
  vec3 get gbr => vec3(y, z, x);
  vec3 get gbg => vec3(y, z, y);
  vec3 get gbb => vec3(y, z, z);
  vec3 get gba => vec3(y, z, w);
  vec3 get gar => vec3(y, w, x);
  vec3 get gag => vec3(y, w, y);
  vec3 get gab => vec3(y, w, z);
  vec3 get gaa => vec3(y, w, w);
  vec3 get brr => vec3(z, x, x);
  vec3 get brg => vec3(z, x, y);
  vec3 get brb => vec3(z, x, z);
  vec3 get bra => vec3(z, x, w);
  vec3 get bgr => vec3(z, y, x);
  vec3 get bgg => vec3(z, y, y);
  vec3 get bgb => vec3(z, y, z);
  vec3 get bga => vec3(z, y, w);
  vec3 get bbr => vec3(z, z, x);
  vec3 get bbg => vec3(z, z, y);
  vec3 get bbb => vec3(z, z, z);
  vec3 get bba => vec3(z, z, w);
  vec3 get bar => vec3(z, w, x);
  vec3 get bag => vec3(z, w, y);
  vec3 get bab => vec3(z, w, z);
  vec3 get baa => vec3(z, w, w);
  vec3 get arr => vec3(w, x, x);
  vec3 get arg => vec3(w, x, y);
  vec3 get arb => vec3(w, x, z);
  vec3 get ara => vec3(w, x, w);
  vec3 get agr => vec3(w, y, x);
  vec3 get agg => vec3(w, y, y);
  vec3 get agb => vec3(w, y, z);
  vec3 get aga => vec3(w, y, w);
  vec3 get abr => vec3(w, z, x);
  vec3 get abg => vec3(w, z, y);
  vec3 get abb => vec3(w, z, z);
  vec3 get aba => vec3(w, z, w);
  vec3 get aar => vec3(w, w, x);
  vec3 get aag => vec3(w, w, y);
  vec3 get aab => vec3(w, w, z);
  vec3 get aaa => vec3(w, w, w);
  vec4 get xxxx => vec4(x, x, x, x);
  vec4 get xxxy => vec4(x, x, x, y);
  vec4 get xxxz => vec4(x, x, x, z);
  vec4 get xxxw => vec4(x, x, x, w);
  vec4 get xxyx => vec4(x, x, y, x);
  vec4 get xxyy => vec4(x, x, y, y);
  vec4 get xxyz => vec4(x, x, y, z);
  vec4 get xxyw => vec4(x, x, y, w);
  vec4 get xxzx => vec4(x, x, z, x);
  vec4 get xxzy => vec4(x, x, z, y);
  vec4 get xxzz => vec4(x, x, z, z);
  vec4 get xxzw => vec4(x, x, z, w);
  vec4 get xxwx => vec4(x, x, w, x);
  vec4 get xxwy => vec4(x, x, w, y);
  vec4 get xxwz => vec4(x, x, w, z);
  vec4 get xxww => vec4(x, x, w, w);
  vec4 get xyxx => vec4(x, y, x, x);
  vec4 get xyxy => vec4(x, y, x, y);
  vec4 get xyxz => vec4(x, y, x, z);
  vec4 get xyxw => vec4(x, y, x, w);
  vec4 get xyyx => vec4(x, y, y, x);
  vec4 get xyyy => vec4(x, y, y, y);
  vec4 get xyyz => vec4(x, y, y, z);
  vec4 get xyyw => vec4(x, y, y, w);
  vec4 get xyzx => vec4(x, y, z, x);
  vec4 get xyzy => vec4(x, y, z, y);
  vec4 get xyzz => vec4(x, y, z, z);
  vec4 get xyzw => vec4(x, y, z, w);
  vec4 get xywx => vec4(x, y, w, x);
  vec4 get xywy => vec4(x, y, w, y);
  vec4 get xywz => vec4(x, y, w, z);
  vec4 get xyww => vec4(x, y, w, w);
  vec4 get xzxx => vec4(x, z, x, x);
  vec4 get xzxy => vec4(x, z, x, y);
  vec4 get xzxz => vec4(x, z, x, z);
  vec4 get xzxw => vec4(x, z, x, w);
  vec4 get xzyx => vec4(x, z, y, x);
  vec4 get xzyy => vec4(x, z, y, y);
  vec4 get xzyz => vec4(x, z, y, z);
  vec4 get xzyw => vec4(x, z, y, w);
  vec4 get xzzx => vec4(x, z, z, x);
  vec4 get xzzy => vec4(x, z, z, y);
  vec4 get xzzz => vec4(x, z, z, z);
  vec4 get xzzw => vec4(x, z, z, w);
  vec4 get xzwx => vec4(x, z, w, x);
  vec4 get xzwy => vec4(x, z, w, y);
  vec4 get xzwz => vec4(x, z, w, z);
  vec4 get xzww => vec4(x, z, w, w);
  vec4 get xwxx => vec4(x, w, x, x);
  vec4 get xwxy => vec4(x, w, x, y);
  vec4 get xwxz => vec4(x, w, x, z);
  vec4 get xwxw => vec4(x, w, x, w);
  vec4 get xwyx => vec4(x, w, y, x);
  vec4 get xwyy => vec4(x, w, y, y);
  vec4 get xwyz => vec4(x, w, y, z);
  vec4 get xwyw => vec4(x, w, y, w);
  vec4 get xwzx => vec4(x, w, z, x);
  vec4 get xwzy => vec4(x, w, z, y);
  vec4 get xwzz => vec4(x, w, z, z);
  vec4 get xwzw => vec4(x, w, z, w);
  vec4 get xwwx => vec4(x, w, w, x);
  vec4 get xwwy => vec4(x, w, w, y);
  vec4 get xwwz => vec4(x, w, w, z);
  vec4 get xwww => vec4(x, w, w, w);
  vec4 get yxxx => vec4(y, x, x, x);
  vec4 get yxxy => vec4(y, x, x, y);
  vec4 get yxxz => vec4(y, x, x, z);
  vec4 get yxxw => vec4(y, x, x, w);
  vec4 get yxyx => vec4(y, x, y, x);
  vec4 get yxyy => vec4(y, x, y, y);
  vec4 get yxyz => vec4(y, x, y, z);
  vec4 get yxyw => vec4(y, x, y, w);
  vec4 get yxzx => vec4(y, x, z, x);
  vec4 get yxzy => vec4(y, x, z, y);
  vec4 get yxzz => vec4(y, x, z, z);
  vec4 get yxzw => vec4(y, x, z, w);
  vec4 get yxwx => vec4(y, x, w, x);
  vec4 get yxwy => vec4(y, x, w, y);
  vec4 get yxwz => vec4(y, x, w, z);
  vec4 get yxww => vec4(y, x, w, w);
  vec4 get yyxx => vec4(y, y, x, x);
  vec4 get yyxy => vec4(y, y, x, y);
  vec4 get yyxz => vec4(y, y, x, z);
  vec4 get yyxw => vec4(y, y, x, w);
  vec4 get yyyx => vec4(y, y, y, x);
  vec4 get yyyy => vec4(y, y, y, y);
  vec4 get yyyz => vec4(y, y, y, z);
  vec4 get yyyw => vec4(y, y, y, w);
  vec4 get yyzx => vec4(y, y, z, x);
  vec4 get yyzy => vec4(y, y, z, y);
  vec4 get yyzz => vec4(y, y, z, z);
  vec4 get yyzw => vec4(y, y, z, w);
  vec4 get yywx => vec4(y, y, w, x);
  vec4 get yywy => vec4(y, y, w, y);
  vec4 get yywz => vec4(y, y, w, z);
  vec4 get yyww => vec4(y, y, w, w);
  vec4 get yzxx => vec4(y, z, x, x);
  vec4 get yzxy => vec4(y, z, x, y);
  vec4 get yzxz => vec4(y, z, x, z);
  vec4 get yzxw => vec4(y, z, x, w);
  vec4 get yzyx => vec4(y, z, y, x);
  vec4 get yzyy => vec4(y, z, y, y);
  vec4 get yzyz => vec4(y, z, y, z);
  vec4 get yzyw => vec4(y, z, y, w);
  vec4 get yzzx => vec4(y, z, z, x);
  vec4 get yzzy => vec4(y, z, z, y);
  vec4 get yzzz => vec4(y, z, z, z);
  vec4 get yzzw => vec4(y, z, z, w);
  vec4 get yzwx => vec4(y, z, w, x);
  vec4 get yzwy => vec4(y, z, w, y);
  vec4 get yzwz => vec4(y, z, w, z);
  vec4 get yzww => vec4(y, z, w, w);
  vec4 get ywxx => vec4(y, w, x, x);
  vec4 get ywxy => vec4(y, w, x, y);
  vec4 get ywxz => vec4(y, w, x, z);
  vec4 get ywxw => vec4(y, w, x, w);
  vec4 get ywyx => vec4(y, w, y, x);
  vec4 get ywyy => vec4(y, w, y, y);
  vec4 get ywyz => vec4(y, w, y, z);
  vec4 get ywyw => vec4(y, w, y, w);
  vec4 get ywzx => vec4(y, w, z, x);
  vec4 get ywzy => vec4(y, w, z, y);
  vec4 get ywzz => vec4(y, w, z, z);
  vec4 get ywzw => vec4(y, w, z, w);
  vec4 get ywwx => vec4(y, w, w, x);
  vec4 get ywwy => vec4(y, w, w, y);
  vec4 get ywwz => vec4(y, w, w, z);
  vec4 get ywww => vec4(y, w, w, w);
  vec4 get zxxx => vec4(z, x, x, x);
  vec4 get zxxy => vec4(z, x, x, y);
  vec4 get zxxz => vec4(z, x, x, z);
  vec4 get zxxw => vec4(z, x, x, w);
  vec4 get zxyx => vec4(z, x, y, x);
  vec4 get zxyy => vec4(z, x, y, y);
  vec4 get zxyz => vec4(z, x, y, z);
  vec4 get zxyw => vec4(z, x, y, w);
  vec4 get zxzx => vec4(z, x, z, x);
  vec4 get zxzy => vec4(z, x, z, y);
  vec4 get zxzz => vec4(z, x, z, z);
  vec4 get zxzw => vec4(z, x, z, w);
  vec4 get zxwx => vec4(z, x, w, x);
  vec4 get zxwy => vec4(z, x, w, y);
  vec4 get zxwz => vec4(z, x, w, z);
  vec4 get zxww => vec4(z, x, w, w);
  vec4 get zyxx => vec4(z, y, x, x);
  vec4 get zyxy => vec4(z, y, x, y);
  vec4 get zyxz => vec4(z, y, x, z);
  vec4 get zyxw => vec4(z, y, x, w);
  vec4 get zyyx => vec4(z, y, y, x);
  vec4 get zyyy => vec4(z, y, y, y);
  vec4 get zyyz => vec4(z, y, y, z);
  vec4 get zyyw => vec4(z, y, y, w);
  vec4 get zyzx => vec4(z, y, z, x);
  vec4 get zyzy => vec4(z, y, z, y);
  vec4 get zyzz => vec4(z, y, z, z);
  vec4 get zyzw => vec4(z, y, z, w);
  vec4 get zywx => vec4(z, y, w, x);
  vec4 get zywy => vec4(z, y, w, y);
  vec4 get zywz => vec4(z, y, w, z);
  vec4 get zyww => vec4(z, y, w, w);
  vec4 get zzxx => vec4(z, z, x, x);
  vec4 get zzxy => vec4(z, z, x, y);
  vec4 get zzxz => vec4(z, z, x, z);
  vec4 get zzxw => vec4(z, z, x, w);
  vec4 get zzyx => vec4(z, z, y, x);
  vec4 get zzyy => vec4(z, z, y, y);
  vec4 get zzyz => vec4(z, z, y, z);
  vec4 get zzyw => vec4(z, z, y, w);
  vec4 get zzzx => vec4(z, z, z, x);
  vec4 get zzzy => vec4(z, z, z, y);
  vec4 get zzzz => vec4(z, z, z, z);
  vec4 get zzzw => vec4(z, z, z, w);
  vec4 get zzwx => vec4(z, z, w, x);
  vec4 get zzwy => vec4(z, z, w, y);
  vec4 get zzwz => vec4(z, z, w, z);
  vec4 get zzww => vec4(z, z, w, w);
  vec4 get zwxx => vec4(z, w, x, x);
  vec4 get zwxy => vec4(z, w, x, y);
  vec4 get zwxz => vec4(z, w, x, z);
  vec4 get zwxw => vec4(z, w, x, w);
  vec4 get zwyx => vec4(z, w, y, x);
  vec4 get zwyy => vec4(z, w, y, y);
  vec4 get zwyz => vec4(z, w, y, z);
  vec4 get zwyw => vec4(z, w, y, w);
  vec4 get zwzx => vec4(z, w, z, x);
  vec4 get zwzy => vec4(z, w, z, y);
  vec4 get zwzz => vec4(z, w, z, z);
  vec4 get zwzw => vec4(z, w, z, w);
  vec4 get zwwx => vec4(z, w, w, x);
  vec4 get zwwy => vec4(z, w, w, y);
  vec4 get zwwz => vec4(z, w, w, z);
  vec4 get zwww => vec4(z, w, w, w);
  vec4 get wxxx => vec4(w, x, x, x);
  vec4 get wxxy => vec4(w, x, x, y);
  vec4 get wxxz => vec4(w, x, x, z);
  vec4 get wxxw => vec4(w, x, x, w);
  vec4 get wxyx => vec4(w, x, y, x);
  vec4 get wxyy => vec4(w, x, y, y);
  vec4 get wxyz => vec4(w, x, y, z);
  vec4 get wxyw => vec4(w, x, y, w);
  vec4 get wxzx => vec4(w, x, z, x);
  vec4 get wxzy => vec4(w, x, z, y);
  vec4 get wxzz => vec4(w, x, z, z);
  vec4 get wxzw => vec4(w, x, z, w);
  vec4 get wxwx => vec4(w, x, w, x);
  vec4 get wxwy => vec4(w, x, w, y);
  vec4 get wxwz => vec4(w, x, w, z);
  vec4 get wxww => vec4(w, x, w, w);
  vec4 get wyxx => vec4(w, y, x, x);
  vec4 get wyxy => vec4(w, y, x, y);
  vec4 get wyxz => vec4(w, y, x, z);
  vec4 get wyxw => vec4(w, y, x, w);
  vec4 get wyyx => vec4(w, y, y, x);
  vec4 get wyyy => vec4(w, y, y, y);
  vec4 get wyyz => vec4(w, y, y, z);
  vec4 get wyyw => vec4(w, y, y, w);
  vec4 get wyzx => vec4(w, y, z, x);
  vec4 get wyzy => vec4(w, y, z, y);
  vec4 get wyzz => vec4(w, y, z, z);
  vec4 get wyzw => vec4(w, y, z, w);
  vec4 get wywx => vec4(w, y, w, x);
  vec4 get wywy => vec4(w, y, w, y);
  vec4 get wywz => vec4(w, y, w, z);
  vec4 get wyww => vec4(w, y, w, w);
  vec4 get wzxx => vec4(w, z, x, x);
  vec4 get wzxy => vec4(w, z, x, y);
  vec4 get wzxz => vec4(w, z, x, z);
  vec4 get wzxw => vec4(w, z, x, w);
  vec4 get wzyx => vec4(w, z, y, x);
  vec4 get wzyy => vec4(w, z, y, y);
  vec4 get wzyz => vec4(w, z, y, z);
  vec4 get wzyw => vec4(w, z, y, w);
  vec4 get wzzx => vec4(w, z, z, x);
  vec4 get wzzy => vec4(w, z, z, y);
  vec4 get wzzz => vec4(w, z, z, z);
  vec4 get wzzw => vec4(w, z, z, w);
  vec4 get wzwx => vec4(w, z, w, x);
  vec4 get wzwy => vec4(w, z, w, y);
  vec4 get wzwz => vec4(w, z, w, z);
  vec4 get wzww => vec4(w, z, w, w);
  vec4 get wwxx => vec4(w, w, x, x);
  vec4 get wwxy => vec4(w, w, x, y);
  vec4 get wwxz => vec4(w, w, x, z);
  vec4 get wwxw => vec4(w, w, x, w);
  vec4 get wwyx => vec4(w, w, y, x);
  vec4 get wwyy => vec4(w, w, y, y);
  vec4 get wwyz => vec4(w, w, y, z);
  vec4 get wwyw => vec4(w, w, y, w);
  vec4 get wwzx => vec4(w, w, z, x);
  vec4 get wwzy => vec4(w, w, z, y);
  vec4 get wwzz => vec4(w, w, z, z);
  vec4 get wwzw => vec4(w, w, z, w);
  vec4 get wwwx => vec4(w, w, w, x);
  vec4 get wwwy => vec4(w, w, w, y);
  vec4 get wwwz => vec4(w, w, w, z);
  vec4 get wwww => vec4(w, w, w, w);
  vec4 get rrrr => vec4(x, x, x, x);
  vec4 get rrrg => vec4(x, x, x, y);
  vec4 get rrrb => vec4(x, x, x, z);
  vec4 get rrra => vec4(x, x, x, w);
  vec4 get rrgr => vec4(x, x, y, x);
  vec4 get rrgg => vec4(x, x, y, y);
  vec4 get rrgb => vec4(x, x, y, z);
  vec4 get rrga => vec4(x, x, y, w);
  vec4 get rrbr => vec4(x, x, z, x);
  vec4 get rrbg => vec4(x, x, z, y);
  vec4 get rrbb => vec4(x, x, z, z);
  vec4 get rrba => vec4(x, x, z, w);
  vec4 get rrar => vec4(x, x, w, x);
  vec4 get rrag => vec4(x, x, w, y);
  vec4 get rrab => vec4(x, x, w, z);
  vec4 get rraa => vec4(x, x, w, w);
  vec4 get rgrr => vec4(x, y, x, x);
  vec4 get rgrg => vec4(x, y, x, y);
  vec4 get rgrb => vec4(x, y, x, z);
  vec4 get rgra => vec4(x, y, x, w);
  vec4 get rggr => vec4(x, y, y, x);
  vec4 get rggg => vec4(x, y, y, y);
  vec4 get rggb => vec4(x, y, y, z);
  vec4 get rgga => vec4(x, y, y, w);
  vec4 get rgbr => vec4(x, y, z, x);
  vec4 get rgbg => vec4(x, y, z, y);
  vec4 get rgbb => vec4(x, y, z, z);
  vec4 get rgba => vec4(x, y, z, w);
  vec4 get rgar => vec4(x, y, w, x);
  vec4 get rgag => vec4(x, y, w, y);
  vec4 get rgab => vec4(x, y, w, z);
  vec4 get rgaa => vec4(x, y, w, w);
  vec4 get rbrr => vec4(x, z, x, x);
  vec4 get rbrg => vec4(x, z, x, y);
  vec4 get rbrb => vec4(x, z, x, z);
  vec4 get rbra => vec4(x, z, x, w);
  vec4 get rbgr => vec4(x, z, y, x);
  vec4 get rbgg => vec4(x, z, y, y);
  vec4 get rbgb => vec4(x, z, y, z);
  vec4 get rbga => vec4(x, z, y, w);
  vec4 get rbbr => vec4(x, z, z, x);
  vec4 get rbbg => vec4(x, z, z, y);
  vec4 get rbbb => vec4(x, z, z, z);
  vec4 get rbba => vec4(x, z, z, w);
  vec4 get rbar => vec4(x, z, w, x);
  vec4 get rbag => vec4(x, z, w, y);
  vec4 get rbab => vec4(x, z, w, z);
  vec4 get rbaa => vec4(x, z, w, w);
  vec4 get rarr => vec4(x, w, x, x);
  vec4 get rarg => vec4(x, w, x, y);
  vec4 get rarb => vec4(x, w, x, z);
  vec4 get rara => vec4(x, w, x, w);
  vec4 get ragr => vec4(x, w, y, x);
  vec4 get ragg => vec4(x, w, y, y);
  vec4 get ragb => vec4(x, w, y, z);
  vec4 get raga => vec4(x, w, y, w);
  vec4 get rabr => vec4(x, w, z, x);
  vec4 get rabg => vec4(x, w, z, y);
  vec4 get rabb => vec4(x, w, z, z);
  vec4 get raba => vec4(x, w, z, w);
  vec4 get raar => vec4(x, w, w, x);
  vec4 get raag => vec4(x, w, w, y);
  vec4 get raab => vec4(x, w, w, z);
  vec4 get raaa => vec4(x, w, w, w);
  vec4 get grrr => vec4(y, x, x, x);
  vec4 get grrg => vec4(y, x, x, y);
  vec4 get grrb => vec4(y, x, x, z);
  vec4 get grra => vec4(y, x, x, w);
  vec4 get grgr => vec4(y, x, y, x);
  vec4 get grgg => vec4(y, x, y, y);
  vec4 get grgb => vec4(y, x, y, z);
  vec4 get grga => vec4(y, x, y, w);
  vec4 get grbr => vec4(y, x, z, x);
  vec4 get grbg => vec4(y, x, z, y);
  vec4 get grbb => vec4(y, x, z, z);
  vec4 get grba => vec4(y, x, z, w);
  vec4 get grar => vec4(y, x, w, x);
  vec4 get grag => vec4(y, x, w, y);
  vec4 get grab => vec4(y, x, w, z);
  vec4 get graa => vec4(y, x, w, w);
  vec4 get ggrr => vec4(y, y, x, x);
  vec4 get ggrg => vec4(y, y, x, y);
  vec4 get ggrb => vec4(y, y, x, z);
  vec4 get ggra => vec4(y, y, x, w);
  vec4 get gggr => vec4(y, y, y, x);
  vec4 get gggg => vec4(y, y, y, y);
  vec4 get gggb => vec4(y, y, y, z);
  vec4 get ggga => vec4(y, y, y, w);
  vec4 get ggbr => vec4(y, y, z, x);
  vec4 get ggbg => vec4(y, y, z, y);
  vec4 get ggbb => vec4(y, y, z, z);
  vec4 get ggba => vec4(y, y, z, w);
  vec4 get ggar => vec4(y, y, w, x);
  vec4 get ggag => vec4(y, y, w, y);
  vec4 get ggab => vec4(y, y, w, z);
  vec4 get ggaa => vec4(y, y, w, w);
  vec4 get gbrr => vec4(y, z, x, x);
  vec4 get gbrg => vec4(y, z, x, y);
  vec4 get gbrb => vec4(y, z, x, z);
  vec4 get gbra => vec4(y, z, x, w);
  vec4 get gbgr => vec4(y, z, y, x);
  vec4 get gbgg => vec4(y, z, y, y);
  vec4 get gbgb => vec4(y, z, y, z);
  vec4 get gbga => vec4(y, z, y, w);
  vec4 get gbbr => vec4(y, z, z, x);
  vec4 get gbbg => vec4(y, z, z, y);
  vec4 get gbbb => vec4(y, z, z, z);
  vec4 get gbba => vec4(y, z, z, w);
  vec4 get gbar => vec4(y, z, w, x);
  vec4 get gbag => vec4(y, z, w, y);
  vec4 get gbab => vec4(y, z, w, z);
  vec4 get gbaa => vec4(y, z, w, w);
  vec4 get garr => vec4(y, w, x, x);
  vec4 get garg => vec4(y, w, x, y);
  vec4 get garb => vec4(y, w, x, z);
  vec4 get gara => vec4(y, w, x, w);
  vec4 get gagr => vec4(y, w, y, x);
  vec4 get gagg => vec4(y, w, y, y);
  vec4 get gagb => vec4(y, w, y, z);
  vec4 get gaga => vec4(y, w, y, w);
  vec4 get gabr => vec4(y, w, z, x);
  vec4 get gabg => vec4(y, w, z, y);
  vec4 get gabb => vec4(y, w, z, z);
  vec4 get gaba => vec4(y, w, z, w);
  vec4 get gaar => vec4(y, w, w, x);
  vec4 get gaag => vec4(y, w, w, y);
  vec4 get gaab => vec4(y, w, w, z);
  vec4 get gaaa => vec4(y, w, w, w);
  vec4 get brrr => vec4(z, x, x, x);
  vec4 get brrg => vec4(z, x, x, y);
  vec4 get brrb => vec4(z, x, x, z);
  vec4 get brra => vec4(z, x, x, w);
  vec4 get brgr => vec4(z, x, y, x);
  vec4 get brgg => vec4(z, x, y, y);
  vec4 get brgb => vec4(z, x, y, z);
  vec4 get brga => vec4(z, x, y, w);
  vec4 get brbr => vec4(z, x, z, x);
  vec4 get brbg => vec4(z, x, z, y);
  vec4 get brbb => vec4(z, x, z, z);
  vec4 get brba => vec4(z, x, z, w);
  vec4 get brar => vec4(z, x, w, x);
  vec4 get brag => vec4(z, x, w, y);
  vec4 get brab => vec4(z, x, w, z);
  vec4 get braa => vec4(z, x, w, w);
  vec4 get bgrr => vec4(z, y, x, x);
  vec4 get bgrg => vec4(z, y, x, y);
  vec4 get bgrb => vec4(z, y, x, z);
  vec4 get bgra => vec4(z, y, x, w);
  vec4 get bggr => vec4(z, y, y, x);
  vec4 get bggg => vec4(z, y, y, y);
  vec4 get bggb => vec4(z, y, y, z);
  vec4 get bgga => vec4(z, y, y, w);
  vec4 get bgbr => vec4(z, y, z, x);
  vec4 get bgbg => vec4(z, y, z, y);
  vec4 get bgbb => vec4(z, y, z, z);
  vec4 get bgba => vec4(z, y, z, w);
  vec4 get bgar => vec4(z, y, w, x);
  vec4 get bgag => vec4(z, y, w, y);
  vec4 get bgab => vec4(z, y, w, z);
  vec4 get bgaa => vec4(z, y, w, w);
  vec4 get bbrr => vec4(z, z, x, x);
  vec4 get bbrg => vec4(z, z, x, y);
  vec4 get bbrb => vec4(z, z, x, z);
  vec4 get bbra => vec4(z, z, x, w);
  vec4 get bbgr => vec4(z, z, y, x);
  vec4 get bbgg => vec4(z, z, y, y);
  vec4 get bbgb => vec4(z, z, y, z);
  vec4 get bbga => vec4(z, z, y, w);
  vec4 get bbbr => vec4(z, z, z, x);
  vec4 get bbbg => vec4(z, z, z, y);
  vec4 get bbbb => vec4(z, z, z, z);
  vec4 get bbba => vec4(z, z, z, w);
  vec4 get bbar => vec4(z, z, w, x);
  vec4 get bbag => vec4(z, z, w, y);
  vec4 get bbab => vec4(z, z, w, z);
  vec4 get bbaa => vec4(z, z, w, w);
  vec4 get barr => vec4(z, w, x, x);
  vec4 get barg => vec4(z, w, x, y);
  vec4 get barb => vec4(z, w, x, z);
  vec4 get bara => vec4(z, w, x, w);
  vec4 get bagr => vec4(z, w, y, x);
  vec4 get bagg => vec4(z, w, y, y);
  vec4 get bagb => vec4(z, w, y, z);
  vec4 get baga => vec4(z, w, y, w);
  vec4 get babr => vec4(z, w, z, x);
  vec4 get babg => vec4(z, w, z, y);
  vec4 get babb => vec4(z, w, z, z);
  vec4 get baba => vec4(z, w, z, w);
  vec4 get baar => vec4(z, w, w, x);
  vec4 get baag => vec4(z, w, w, y);
  vec4 get baab => vec4(z, w, w, z);
  vec4 get baaa => vec4(z, w, w, w);
  vec4 get arrr => vec4(w, x, x, x);
  vec4 get arrg => vec4(w, x, x, y);
  vec4 get arrb => vec4(w, x, x, z);
  vec4 get arra => vec4(w, x, x, w);
  vec4 get argr => vec4(w, x, y, x);
  vec4 get argg => vec4(w, x, y, y);
  vec4 get argb => vec4(w, x, y, z);
  vec4 get arga => vec4(w, x, y, w);
  vec4 get arbr => vec4(w, x, z, x);
  vec4 get arbg => vec4(w, x, z, y);
  vec4 get arbb => vec4(w, x, z, z);
  vec4 get arba => vec4(w, x, z, w);
  vec4 get arar => vec4(w, x, w, x);
  vec4 get arag => vec4(w, x, w, y);
  vec4 get arab => vec4(w, x, w, z);
  vec4 get araa => vec4(w, x, w, w);
  vec4 get agrr => vec4(w, y, x, x);
  vec4 get agrg => vec4(w, y, x, y);
  vec4 get agrb => vec4(w, y, x, z);
  vec4 get agra => vec4(w, y, x, w);
  vec4 get aggr => vec4(w, y, y, x);
  vec4 get aggg => vec4(w, y, y, y);
  vec4 get aggb => vec4(w, y, y, z);
  vec4 get agga => vec4(w, y, y, w);
  vec4 get agbr => vec4(w, y, z, x);
  vec4 get agbg => vec4(w, y, z, y);
  vec4 get agbb => vec4(w, y, z, z);
  vec4 get agba => vec4(w, y, z, w);
  vec4 get agar => vec4(w, y, w, x);
  vec4 get agag => vec4(w, y, w, y);
  vec4 get agab => vec4(w, y, w, z);
  vec4 get agaa => vec4(w, y, w, w);
  vec4 get abrr => vec4(w, z, x, x);
  vec4 get abrg => vec4(w, z, x, y);
  vec4 get abrb => vec4(w, z, x, z);
  vec4 get abra => vec4(w, z, x, w);
  vec4 get abgr => vec4(w, z, y, x);
  vec4 get abgg => vec4(w, z, y, y);
  vec4 get abgb => vec4(w, z, y, z);
  vec4 get abga => vec4(w, z, y, w);
  vec4 get abbr => vec4(w, z, z, x);
  vec4 get abbg => vec4(w, z, z, y);
  vec4 get abbb => vec4(w, z, z, z);
  vec4 get abba => vec4(w, z, z, w);
  vec4 get abar => vec4(w, z, w, x);
  vec4 get abag => vec4(w, z, w, y);
  vec4 get abab => vec4(w, z, w, z);
  vec4 get abaa => vec4(w, z, w, w);
  vec4 get aarr => vec4(w, w, x, x);
  vec4 get aarg => vec4(w, w, x, y);
  vec4 get aarb => vec4(w, w, x, z);
  vec4 get aara => vec4(w, w, x, w);
  vec4 get aagr => vec4(w, w, y, x);
  vec4 get aagg => vec4(w, w, y, y);
  vec4 get aagb => vec4(w, w, y, z);
  vec4 get aaga => vec4(w, w, y, w);
  vec4 get aabr => vec4(w, w, z, x);
  vec4 get aabg => vec4(w, w, z, y);
  vec4 get aabb => vec4(w, w, z, z);
  vec4 get aaba => vec4(w, w, z, w);
  vec4 get aaar => vec4(w, w, w, x);
  vec4 get aaag => vec4(w, w, w, y);
  vec4 get aaab => vec4(w, w, w, z);
  vec4 get aaaa => vec4(w, w, w, w);
}
